server:
  port: 8761
spring:
  profiles:
    active: peer1
  application:
    name: EurekaServer1
eureka:
  instance:
    hostname: peer1
  client:
#    register-with-eureka: false
#    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8762/eureka/

