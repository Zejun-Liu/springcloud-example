server:
  port: 8762
spring:
  profiles:
    active: peer2
  application:
    name: EurekaServer2
eureka:
  instance:
    hostname: peer2
  client:
#    register-with-eureka: false
#    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
